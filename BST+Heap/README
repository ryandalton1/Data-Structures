This code takes in a txt file of movies. It will create a binary search tree where each node is a letter.

When a movie is inserted it will go to the corresponding first letter and add it to a heap at that node.
